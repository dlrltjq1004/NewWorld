using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class CharacterMove : MonoBehaviour {

    JoyStick joy;
    Network network;
    
	// Use this for initialization
	void Start () {
        //joy = new JoyStick();
        //network = new Network();

        


    }
	
	// Update is called once per frame
	void Update () {

        //if(joy.MoveFlag==true)
        //{
        //    string playerMove = joy.Player.transform.position.ToString();
        //    network.Send(playerMove);
        //}


        
	}
}
